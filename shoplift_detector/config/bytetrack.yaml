# Ultralytics ByteTrack tracker config for store camera streams.
tracker_type: bytetrack
track_high_thresh: 0.6
track_low_thresh: 0.1
new_track_thresh: 0.7
track_buffer: 60
match_thresh: 0.8
fuse_score: true
