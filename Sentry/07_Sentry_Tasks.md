# Sentry — Task Board
## Chipmo → Sentry v1.1 Cloud-Only SaaS Tasks

**Баримтын дугаар:** DOC-07
**Хувилбар:** 1.0
**Огноо:** 2026-04-22
**Эзэмшигч:** Lil
**Холбогдох:** `06_Sentry_MVP_Roadmap.md`, `01_Sentry_PRD_v1.1.md`, `05_Sentry_Multi_Tenancy_Architecture.md`

---

## Статусын тэмдэглэгээ

- `todo` — эхлээгүй
- `wip` — хийж байна
- `blocked` — гадны шалтгаанаар хүлээж байна
- `done` — дууссан + тест OK
- `cancelled` — хэрэггүй болсон

## Priority

- **P0** — pilot launch дотор заавал (MVP blocker)
- **P1** — Phase 1 эцэс хүртэл (growth-д шаардагдана)
- **P2** — Phase 2-д хамаарна
- **P3** — Phase 3 / nice-to-have

---

## Phase 1 — MVP (Months 1–3)

### WS-1: Tenant foundation

| ID | Title | Priority | Depends on | Status | Definition of Done |
|---|---|---|---|---|---|
| T1-01 | UUID `tenants` хүснэгт + migration | P0 | — | done | Migration `20260422_01_add_tenants.py` (down_rev `20260421_09`), model `shoplift_detector/app/db/models/tenant.py` (`TENANT_STATUSES`, `TENANT_PLANS`), test `tests/test_tenants_migration.py` (revision chain, columns, CHECK/UNIQUE constraints, downgrade). |
| T1-02 | `organizations.id → tenants.tenant_id` map table | P0 | T1-01 | done | Migration `20260422_02_add_organization_tenant_map.py` (map table + `organization_tenants` view + idempotent backfill). Model `shoplift_detector/app/db/models/organization_tenant_map.py`. Test `tests/test_organization_tenant_map.py`. |
| T1-03 | Бүх tenant-scoped хүснэгтэд `tenant_id UUID` багана нэмэх | P0 | T1-01 | done | Migration `20260422_03_add_tenant_id_columns.py` (8 table: stores/cameras/alerts/alert_feedback/cases/sync_packs/inference_metrics/camera_health). FK → `tenants(tenant_id) ON DELETE CASCADE` + btree partial index `idx_{table}_tenant` + map-based backfill. (GIN-ийн оронд btree — UUID нэг утгын баганад GIN нь `btree_gin` extension шаардана.) Test `tests/test_tenant_id_columns_migration.py`. |
| T1-04 | PostgreSQL RLS policy бүх tenant table дээр идэвхжүүлэх | P0 | T1-03 | done | Migration `20260422_04_enable_tenant_rls.py` — `ENABLE` + `FORCE ROW LEVEL SECURITY` + `tenant_isolation` policy (USING+WITH CHECK) бүх 8 table дээр. Policy body: `bypass_tenant='on'` OR `tenant_id = current_tenant_id::uuid` (fail-closed when GUC empty). Config flag `TENANCY_RLS_ENFORCED` (default false) `shoplift_detector/app/core/config.py`. Test `tests/test_tenant_rls_migration.py`. |
| T1-05 | `get_current_tenant` FastAPI dependency | P0 | T1-01 | done | `shoplift_detector/app/core/tenant_auth.py` + `shoplift_detector/app/db/repository/tenants.py` (`TenantRepository.get_by_api_key_hash`, `hash_api_key` SHA-256). Bearer prefix validation → hash → DB lookup → 401 (same body for unknown/pending/suspended/grace/churned). Test `tests/test_tenant_auth.py`. |
| T1-06 | Per-tenant API key generator + rotation | P0 | T1-05 | done | Migration `20260422_05_add_api_key_rotation_columns.py` (`previous_api_key_hash` + `previous_api_key_expires_at`). Service `shoplift_detector/app/services/api_key_service.py` (`generate_api_key` urlsafe_b64 32-byte, `rotate_api_key` 24h overlap). `TenantRepository.rotate_api_key` + `clear_expired_rotation_keys` sweeper. `get_by_api_key_hash` now OR's previous hash within TTL. Endpoint `POST /api/v1/tenants/me/api-keys/rotate`. Test `tests/test_api_key_service.py`. |
| T1-07 | Resource quota enforcement (camera/store limits) | P0 | T1-05 | done | `shoplift_detector/app/core/quota.py` (`PLAN_QUOTA_DEFAULTS`: Starter 5/1, Pro 50/10, Enterprise ∞/∞). `QuotaExceededError` (403 + `upgrade_url` + Mongolian message). Helpers: `ensure_camera_quota`, `ensure_store_quota`, `ensure_can_add`. JSONB numeric-string coercion. Test `tests/test_quota.py`. |
| T1-08 | API call rate limit per tenant (token bucket Redis) | P1 | T1-05 | done | `shoplift_detector/app/core/tenant_rate_limit.py` — `TenantRateLimiter` with pluggable `RateLimitBackend`, `InMemoryBackend` default (Redis замнал T1-12-д). Plan limits: Trial/Starter 30, Pro 60, Enterprise 600 per минут. `enforce()` → 429 + `Retry-After`. Tenant/action scoped bucket keys. FastAPI dep `enforce_tenant_rate_limit`. Test `tests/test_tenant_rate_limit.py`. |
| T1-09 | GPU inference queue priority (plan-based) | P1 | T1-01 | done | `shoplift_detector/app/services/inference_queue.py` — `PriorityInferenceQueue` (`heapq` + FIFO tie-break). `PLAN_PRIORITY`: enterprise=0, pro=1, starter=2, trial=3. Unknown plan → trial priority. `asyncio.Event` blocking `get()`. Test `tests/test_inference_queue.py`. |
| T1-10 | Tenant lifecycle state machine | P0 | T1-01 | done | `shoplift_detector/app/services/tenant_lifecycle.py` — `VALID_TRANSITIONS` graph (pending→active, active→suspended/grace, suspended→active/grace/churned, grace→active/churned, churned terminal). `transition_tenant_status()` атомар UPDATE + audit_log `tenant_status_change` action + commit. `InvalidTransitionError` 409 + `allowed_next` массив. 400 unknown status, 404 missing tenant. Test `tests/test_tenant_lifecycle.py`. |
| T1-11 | 90-day grace + data purge cron | P0 | T1-10 | done | Migration `20260422_06_add_status_changed_at.py` (+ `idx_tenants_churned_purge` partial). `transition_tenant_status` UPDATE одоо `status_changed_at = now()`. Service `shoplift_detector/app/services/tenant_purge.py` — `find_purge_candidates` (90d cutoff + NULL-skip fail-closed), `purge_tenant` (Qdrant collection drop, MinIO prefix wipe, SQL DELETE 8 хүснэгт, null hashes, audit `tenant_purge` action), `run_purge_cron` loop, pluggable `QdrantLike` / `ObjectStoreLike` protocols. Test `tests/test_tenant_purge.py`. |
| T1-12 | Redis key namespace helper class | P1 | T1-01 | done | `shoplift_detector/app/core/tenant_keys.py` — `TenantKeys` frozen dataclass + `_canonicalize` UUID validation (rejects empty/whitespace/non-UUID, `TypeError` on int). Key builders: `person_state`, `camera_state`, `store_scope`, `rate_limit`, `reid_collection_name`. Empty-segment guard prevents global-key collisions. Test `tests/test_tenant_keys.py`. |
| T1-13 | Per-tenant MinIO bucket prefix | P1 | T1-01 | done | `shoplift_detector/app/core/tenant_storage.py` — `TenantBucketLayout` (`tenant_{uuid}/store_{id}/YYYY-MM-DD/event_*.ext`). Path-traversal sanitizer (`../` → `_`, `.`/`..` rejected). `key_belongs_to_tenant` ownership check. `iam_policy()` S3/MinIO template (explicit actions, prefix-scoped Resource + StringLike condition). Test `tests/test_tenant_storage.py`. |
| T1-14 | Query linter: ensure `tenant_id` filter | P1 | T1-04 | done | `tools/tenant_query_linter.py` — AST walk for `text("...")` literals, checks against 8 tenant-scoped tables, accepts `-- NO_TENANT_SCOPE` opt-out marker. Skips `tests/`, `tools/`, `alembic/`. `lint_file` + `lint_tree` + CLI `main()` exit 1 on violations. Test `tests/test_tenant_query_linter.py` (per-table param tests + repo smoke test). |
| T1-15 | Cross-tenant IDOR pen test automation | P0 | T1-04 | done | `tests/test_cross_tenant_idor_pen.py` — 6 attacker scenarios: forged API key → 401, Redis key collision impossible, MinIO path traversal rejected, rate-limit bucket isolation, quota per-tenant, lifecycle transition mutates only targeted tenant + failed transitions leave no partial writes. |

### WS-2: Self-service onboarding (FR-12)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T2-01 | `POST /api/v1/auth/signup` (email + phone + store name) | P0 | T1-01 | done | Migration `20260422_07_add_onboarding_and_otp.py` нэмсэн `onboarding_step` (pending_email/pending_plan/pending_payment/completed) + `email_verified_at` + `phone_verified_at`. Endpoint `POST /api/v1/auth/signup` (`shoplift_detector/app/api/v1/onboarding.py`). Service `signup_service.py` — tenant row `status=pending / onboarding_step=pending_email` + api_key сансрын давхар hash + issue email/SMS OTP. Phone normalizer `shoplift_detector/app/core/phone_format.py` (`+976` canonical). 409 on duplicate email. Test `tests/test_signup_service.py` + `test_phone_format.py`. |
| T2-02 | Email OTP (Resend integration) | P0 | T2-01 | done | `shoplift_detector/app/services/otp_service.py` — 6-digit `secrets.randbelow(10**6)`, SHA-256 hash + `hmac.compare_digest`, 15-мин TTL, 3 attempts → `OtpExhausted`, `issue_otp` + `verify_otp`. `email_sender.py` `EmailSender` protocol + `ResendEmailSender` (httpx) + `RecordingEmailSender` fallback + `build_otp_email` Mongolian template. Config: `RESEND_API_KEY`. Test `tests/test_otp_service.py` + `test_email_and_sms_senders.py`. |
| T2-03 | SMS OTP (Twilio) | P1 | T2-02 | done | `shoplift_detector/app/services/sms_sender.py` — `SmsSender` protocol + `TwilioSmsSender` (REST API httpx + Basic auth) + `RecordingSmsSender` fallback + `build_otp_sms` single-segment Mongolian body. `SmsUnavailableError` триггердэхэд signup_service email-ээр fallback болно (`test_signup_service.py::test_signup_falls_back_to_email_when_sms_provider_unavailable`). Config: `TWILIO_ACCOUNT_SID/AUTH_TOKEN/FROM_NUMBER`. |
| T2-04 | `POST /api/v1/auth/verify-otp` | P0 | T2-02 | done | Endpoint в `shoplift_detector/app/api/v1/onboarding.py`. Email lookup (case-insensitive) → `verify_otp` → `TenantRepository.mark_email_verified` (atomic: sets `email_verified_at` + advances `onboarding_step` from `pending_email` → `pending_plan`). Uniform 400 `invalid_verification` for all failure paths so attacker cannot enumerate emails. |
| T2-05 | `/signup` + `/verify` + `/plan` React pages | P0 | T2-01 | done | `security-web/src/pages/Onboarding/SignupPage.jsx` (Tailwind v4, client-side Mongolian phone regex + field validation). `VerifyPage.jsx` (6-digit split input + paste support + backspace navigation). `PlanPage.jsx` (camera/store sliders + location + annual prepay toggle, recommended badge). `App.jsx`-д `/signup`, `/verify`, `/plan` route нэмсэн. |
| T2-06 | Plan picker logic (auto-recommend by camera count) | P0 | T2-05 | done | `shoplift_detector/app/services/plan_recommender.py` — `recommend_plan` (1-5→starter, 6-50→pro, 51+→enterprise, 10+ store→enterprise upshift), `build_picker` (three-card payload, per-tier camera clamp for fair comparison, `ANNUAL_DISCOUNT_PCT=0.10`). Endpoint `GET /api/v1/onboarding/plan-picker`. `PLAN_FEATURES` catalog (Mongolian). Test `tests/test_plan_recommender.py`. |
| T2-07 | 14-day trial activation (skip payment path) | P0 | T2-06, T1-10 | done | `shoplift_detector/app/services/trial_service.py` — `activate_trial()` атомар UPDATE (status=active, plan=trial, trial_ends_at=+14d, onboarding_step=pending_payment, resource_quota=TRIAL_ACTIVE_QUOTA с 5-cam cap + Pro-level бусад, api_key_hash overwrite). WHERE-clause race-guard pre-activation state дээр pin-дэг; rowcount=0 → `TrialAlreadyActive`. Audit `trial_activated` action. Endpoint `POST /api/v1/onboarding/activate-trial` буцаана raw API key (one-time). Test `tests/test_trial_service.py` (12 тест). |
| T2-08 | Onboarding email sequence (7-day) | P1 | T2-07 | done | `shoplift_detector/app/services/onboarding_emails.py` — 7 template (Day 0/1/2/3/5/7/12) монгол хэлээр. `due_for_tenant()` backfill хийхгүй (Day 4-т миссдсэн Day 1 дахин илгээгдэхгүй). Audit log `onboarding_email_sent` action + `details.day`-д тулгуурласан history. `dispatch_due_emails()` + `run_onboarding_email_cron()` (cron target — trial-active tenant-уудыг 14d window-той нэгжинэ). Test `tests/test_onboarding_emails.py` (22 тест). |
| T2-09 | PostHog event instrumentation | P1 | T2-05 | done | Frontend `security-web/src/services/analytics.js` — lazy PostHog loader, `ANALYTICS_EVENTS` catalog, `trackEvent` fire-and-forget (no-op when `VITE_POSTHOG_KEY` unset). SignupPage / VerifyPage / PlanPage instrumented (`signup_started`, `signup_completed`, `email_verified`, `plan_selected`). Backend `shoplift_detector/app/services/analytics.py` — `PostHogClient` (httpx) + `NullAnalyticsClient` recorder + `build_analytics_client()` factory + module-singleton `capture()`. Config: `POSTHOG_API_KEY`, `POSTHOG_HOST`. Test `tests/test_analytics_backend.py` (6 тест). |
| T2-10 | Onboarding funnel dashboard | P1 | T2-09 | done | `observability/posthog/onboarding-funnel.json` — 9-step FUNNELS insight + trend + `p50(time_to_first_detection)` tile (30d window). `observability/posthog/onboarding-dropoff-alert.yml` — Slack alert subscription: алхам бүрт `<0.60` conversion warning, `trial_activated → first_detection` critical (15-мин SLO), `p50 > 900s` guard. `observability/posthog/README.md` — posthog-cli import guide. Test `tests/test_posthog_dashboard_config.py` (10 тест — funnel order, severity, threshold). |
| T2-11 | "Stuck?" live chat integration (Crisp / Intercom) | P1 | T2-05 | done | `security-web/src/services/liveChat.js` — lazy Crisp widget loader (VITE_CRISP_WEBSITE_ID шалгана, missing үед no-op), `openChat()` сессийг `onboarding:{step}` segment-аар тэмдэглэнэ, `showStuckPromptOnce()` нь idle detection-ээс давтагдалгүй байлгах гvard. `security-web/src/hooks/useIdleTimer.js` — 5 DOM event listener-тай hook, `[isIdle, reset]` tuple. PlanPage-т `IDLE_PROMPT_MS=5m` + `showStuckPromptOnce({step:'plan'})` wired. Config: `CRISP_WEBSITE_ID`. |
| T2-12 | First-detection celebration UI | P0 | T2-05, T7-01 | done | `security-web/src/pages/Onboarding/ReadyPage.jsx` — visibility-aware `LivePreview` (MJPEG via `getVideoFeedUrlV2`, ажиллахгүй үед `PreviewFallback`), "🎉 Бэлэн боллоо!" гарчиг, 3 алхмын checklist (live_preview / telegram / team), `FIRST_DETECTION` + `ONBOARDING_COMPLETED` analytics. Route `/ready` `App.jsx`-д холбогдсон. Telegram холбох / Dashboard / bot-г Telegram дээр нээх 3 CTA. |
| T2-13 | Welcome email with Telegram bot link | P0 | T2-04 | done | `shoplift_detector/app/services/onboarding_emails.py::day_0_welcome` — Resend-д илгээх `OutgoingEmail` template (монгол body + HTML wrapper) `_telegram_bot_link()` ашиглан `https://t.me/sentry_bot?start={tenant_id}` линк суулгасан. Day 0-д `dispatch_due_emails()` автоматаар илгээж `audit_log` (`onboarding_email_sent`) бичнэ. Test `tests/test_onboarding_emails.py` — `t.me/sentry_bot?start={tenant_id}` assertion агуулсан. |

### WS-3: Billing & subscription (FR-13)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T3-01 | QPay checkout session create endpoint | P0 | T1-01 | todo | `POST /api/v1/billing/checkout-session` → QPay invoice URL + QR. |
| T3-02 | QPay webhook receiver | P0 | T3-01 | todo | `POST /api/v1/billing/webhook/qpay` → verify signature → tenant.status=`active`. Idempotent. |
| T3-03 | Stripe checkout session create endpoint | P0 | T1-01 | todo | `POST /api/v1/billing/checkout-session` with provider=`stripe`. Subscription mode. |
| T3-04 | Stripe webhook receiver | P0 | T3-03 | todo | `invoice.paid`, `customer.subscription.updated`, `customer.subscription.deleted`. Signature verify. |
| T3-05 | `usage_events` хүснэгт + daily aggregator cron | P0 | T1-01 | todo | `(tenant_id, metric, value, recorded_at, store_id)` — metrics: camera_active, gpu_seconds, storage_gb, api_call. 02:00 UTC cron. |
| T3-06 | Monthly invoice generator (PDF, Mongolian) | P0 | T3-05 | todo | reportlab / weasyprint template. НӨАТ 10% line. Stored in MinIO. URL in customer portal. |
| T3-07 | Failed payment retry schedule | P0 | T3-02, T3-04 | todo | Day 3, 7, 14 retry. After 3rd fail → status `suspended`. Email reminder on each attempt. |
| T3-08 | Plan upgrade/downgrade endpoint | P0 | T3-03 | todo | `PATCH /api/v1/tenants/me/plan`. Prorate. Camera count auto-adjust billing. |
| T3-09 | Camera count auto-adjust billing | P1 | T3-08 | todo | On camera add/remove: write `usage_events` row → next invoice reflects new count. |
| T3-10 | Cancellation flow + 90-day data export | P0 | T1-10 | todo | `DELETE /api/v1/tenants/me` → status `grace`. Customer portal "Export all data" button. |
| T3-11 | Pricing calculator endpoint (already exists — extend with НӨАТ) | P1 | — | wip | `/api/v1/pricing/quote` — add `vat: 10%` and `total_with_vat`. |

### WS-4: Docker agent + ONVIF (FR-1, FR-12)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T4-01 | Sentry agent Dockerfile (Python + OpenCV + ONVIF lib) | P0 | — | done | Шинэ `agent/` root: `sentry_agent` Python package (config loader env-over-YAML precedence-тэй, argparse CLI с `run` / `version` subcommand, signal-aware runner stub, `redact()` log-safe config dump). Deps pinned `pyproject.toml`: `opencv-python-headless==4.10.0.84`, `onvif-zeep==0.2.12`, `httpx==0.27.2`, `PyYAML==6.0.2`. `agent/Dockerfile` — multi-stage (builder venv → slim runtime), `python:3.11-slim` base, runtime libs `libgl1`/`libglib2.0-0`/`libgomp1`/`tini`, non-root user `sentry` (uid 10001), OCI labels (title/source/revision/created/version), `__pycache__` + `tests/` purge в builder-ээс runtime-д орохоос өмнө. Орчин: ~160 MB (base 45 + opencv-headless 60 + numpy 20 + zeep/lxml 15 + runtime libs 15) — 500 MB budget-оос сайн доор. CI workflow `.github/workflows/agent-release.yml` — `docker/build-push-action@v6` GHCR push (`ghcr.io/${owner}/sentry-agent`), `docker/metadata-action@v5` tag matrix (branch / semver prefix `agent-v` / sha / latest on main), `sigstore/cosign-installer@v3` + keyless `cosign sign` OIDC-ээр, `cosign verify` identity-regex-ээр selbsttest хийнэ. `IMAGE_SIZE_LIMIT_MB=500` размер-гаалий (fail-closed). PR-д build + smoke `version` subcommand, main/tag-д push+sign. `tini` ENTRYPOINT SIGTERM forwarding + zombie reap. Тест `tests/test_sentry_agent_config.py` (11 тест — env+YAML precedence, missing-all, invalid heartbeat, redact masking, CLI version/run/error exit 2). |
| T4-02 | Agent config.yaml template с embedded API key | P0 | T1-06 | done | `shoplift_detector/app/services/installer_config.py` — stateless S3-presigned-style флоу. `build_config_yaml()` 4 скаляр талбарыг (server_url / tenant_id / api_key / heartbeat_interval_s) hand-render хийнэ (PyYAML dep нэмэхгүй); `\n` + `"` character guard-аар malicious tenant injection хаана. `InstallerTokenPayload` (version=1, tenant_id, api_key, server_url, expires_at) → `sign_download_token()` sort_keys JSON → base64url → `<payload>.<sig>` HMAC-SHA256 `settings.SECRET_KEY`-ээр; `verify_download_token()` `hmac.compare_digest` timing-safe + TTL+version guard. `issue_installer_token()` TTL=24h default. Endpoint цэгүүд `shoplift_detector/app/api/v1/installer.py`: `POST /api/v1/installer/config` (authenticated `CurrentTenant`) — T1-06-ийн `rotate_api_key()`-гээр key rotate хийж шинэ raw key URL-д embed, `previous_api_key_valid_until` нэмж буцаана (24h overlap). `GET /api/v1/installer/config/{token}` (unauthenticated, token-signed) — `text/yaml` `Content-Disposition: attachment; filename="config.yaml"` `Cache-Control: no-store` header-тэй stream. Verification failure бүгдийг ганц **410 Gone** болгож (expired / tampered / malformed distinguishable биш — enumeration хаалт). Response schema `shoplift_detector/app/schemas/installer.py::InstallerConfigUrlResponse`. Router wiring `app/api/v1/__init__.py` prefix `/installer` tag `Installer`. Server URL `settings.PUBLIC_BASE_URL`-аас авна, fallback `https://api.sentry.mn`. Тест `tests/test_installer_config.py` (26 тест) — YAML field shape / trailing slash / illegal char reject; token roundtrip / tamper payload+sig / wrong secret / expiry / malformed shapes / version guard; `issue_installer_token` TTL 24h; `build_download_url`; end-to-end POST→GET FastAPI TestClient с FakeDB (tenant_override + db_override) — UPDATE tenants rotate хийгддэг, GET YAML body `tenant_id/api_key/server_url` агуулдаг, attachment headers зөв; 410 on invalid + expired. |
| T4-03 | Windows `.exe` installer bundle (Inno Setup / NSIS) | P0 | T4-01 | done | Шинэ `agent/installer/windows/` бүрээс — Inno Setup 6 script `SentryAgent.iss` (AppId GUID, `PrivilegesRequired=admin` UAC, `ArchitecturesAllowed=x64` 64-bit gate, `MinVersion=10.0.17763` Windows 10 1809 WSL2 requirement, `WizardStyle=modern`, parameterized `#define SentryVersion`). `[Code]` Pascal section `DetectDockerDesktop()` — `HKLM\SOFTWARE\Docker Inc.\Docker Desktop` registry probe + `{autopf}\Docker\Docker\Docker Desktop.exe` filesystem fallback. `InitializeSetup()` — Docker Desktop байхгүй бол Монгол YesNo prompt гаргана. `InitializeWizard()` — T4-02 signed URL-ийг config.yaml-ыг татаж авахын тулд `CreateInputQueryPage` wizard page (`/CONFIGURL` CLI param-аас fallback). `[Files]` — 3 PS1 + README; `[Dirs]` — `{commonappdata}\Sentry\Agent` Admins+SYSTEM full-control ACL; `[Run]` — PowerShell -ээр bootstrap.ps1 runhidden waituntilterminated дуудна; `[UninstallRun]` — uninstall.ps1 RunOnceId. PowerShell 3 script: `bootstrap.ps1` (Start-BitsTransfer + Invoke-WebRequest fallback docker installer, `--quiet --accept-license` silent install, 300s docker engine wait, config.yaml download + Admin-only ACL hardening, `docker pull`, `Register-ScheduledTask` SYSTEM RestartInterval 1m AtStartup trigger Start-Scheduled). `service-wrapper.ps1` — foreground docker run --init (ContainerName `sentry-agent`, config /etc/sentry-agent/config.yaml:ro mount), volumeSpec string-concat form `${var}:` parser collision-ийг тойрсон. `uninstall.ps1` — `Unregister-ScheduledTask` + `docker rm -f` + `docker image rm`, ProgramData config orхигдсон (reinstall retention). CI workflow `.github/workflows/agent-installer-windows.yml` — windows-latest runner, chocolatey-aар Inno Setup 6.3.3 install, ISCC lint + compile, signtool Authenticode sign `secrets.WINDOWS_CERT_BASE64` + `WINDOWS_CERT_PASSWORD`-гоор conditional (PR forks fail хийхгүй), DigiCert RFC 3161 timestamp + SHA-256 digest, `signtool verify /pa` self-test, artifact upload + tag push-д `softprops/action-gh-release@v2`-ээр release asset. Тест `tests/test_windows_installer_bundle.py` (39 тест): .iss directive guards (UAC/x64/MinVersion/parameterized version/5 sections), [Run] powershell pipe, Docker detection signature, ProgramData ACL directive, CONFIGURL wizard, bootstrap.ps1 semantic checks (mandatory params, Test-DockerDesktopInstalled, Install-DockerDesktop silent install, docker engine wait, Register-ScheduledTask SYSTEM, ACL tightening, transcript logging), 3 PS1 файл `System.Management.Automation.Language.Parser::ParseFile` шалгалт (subprocess +  -File harness), workflow YAML shape (windows-latest runner, ISCC call, sign_decision gated `secrets.WINDOWS_CERT_BASE64`, release asset on tag). |
| T4-04 | Linux `.sh` installer bundle | P0 | T4-01 | done | Шинэ `agent/installer/linux/` бүрээс — `install.sh` POSIX-ish bash `set -Eeuo pipefail`, `--config-url` / `--image` / `--no-verify-cosign` / `--uninstall` флаг + `SENTRY_CONFIG_URL` / `SENTRY_IMAGE` env fallback, root guard (`id -u`), `detect_docker()` + `install_docker()` via `https://get.docker.com` + `systemctl enable --now docker`, optional `cosign verify` keyless chain (`certificate-identity-regexp` repo workflow-д pin, `--no-verify-cosign` air-gapped escape hatch), `download_config()` атомар `mktemp` → `curl -fsSL --retry 3` → `chmod 0600 root:root` → `mv -f` (half-written config хаах), `write_systemd_unit()` local tarball path (sed `@SENTRY_IMAGE@` replace) + `curl|bash` heredoc fallback, `daemon-reload` + `enable` + `restart`. `uninstall()` `stop/disable/rm` + `docker rm -f` + `docker image rm`, `/etc/sentry-agent` хадгалж reinstall retention хангана. `uninstall.sh` — 5 мөр wrapper, `install.sh --uninstall`-руу `exec`. `sentry-agent.service` systemd unit — `Requires=docker.service After=docker.service network-online.target`, `Type=simple Restart=always RestartSec=10 TimeoutStart=300`, `ExecStartPre=-/usr/bin/docker rm -f` stale cleanup, `ExecStart=/usr/bin/docker run --init --rm --name --pull always -v config:ro @SENTRY_IMAGE@`, hardening `NoNewPrivileges=true ProtectSystem=strict ReadWritePaths=/var/log/sentry-agent /var/lib/sentry-agent /var/run ProtectHome=true`, `WantedBy=multi-user.target`. CI workflow `.github/workflows/agent-installer-linux.yml` — validate job: `ludeeus/action-shellcheck@master` `-S warning`, `bash -n` syntax loop, `systemd-analyze verify` (staged copy + `@SENTRY_IMAGE@` placeholder rewrite). package job: per-file `*.sha256` + combined `SHA256SUMS` manifest, GPG detach-sign conditional `secrets.GPG_PRIVATE_KEY` (pinentry-mode loopback), artifact upload + tag `softprops/action-gh-release@v2` 8 файл attach (install/uninstall/unit + 3 sha256 sidecars + SHA256SUMS + SHA256SUMS.asc). README — `sha256sum -c install.sh.sha256` checksum-verified install pattern + `curl \| sudo bash` one-liner + flag reference. Тест `tests/test_linux_installer_bundle.py` (44 тест): install.sh strict-mode / 5 flag / 2 env fallback / Docker detect+install / cosign verify / 0600+root:root+mktemp+mv -f atomic write / systemd unit path interpolation + daemon-reload + enable + restart / uninstall preserves config (no `rm -rf /etc/sentry-agent`) / root guard / `bash -n`; uninstall.sh delegate; systemd unit 3 section + 10 directive + 4 hardening + `@SENTRY_IMAGE@` placeholder + ro mount; README sha256 docs; workflow triggers + shellcheck+systemd-analyze validate job + SHA256SUMS emit + GPG secret-gated + release asset shape. |
| T4-05 | macOS `.pkg` installer | P1 | T4-01 | done | Шинэ `agent/installer/macos/` бүрээс. `build.sh` Apple stock `pkgbuild` + `productbuild` toolchain (no 3rd-party deps) — `--version` / `--image` / `--sign-identity` / `--output-dir` флаг, payload layout `/usr/local/sentry-agent/run-agent.sh` + `/Library/LaunchDaemons/mn.sentry.agent.plist` + `@SENTRY_IMAGE@` sed substitution, component pkg `mn.sentry.agent` identifier + `--install-location /`, productbuild distribution.xml (min-os Monterey, rootVolumeOnly, non-customizable), optional `productsign "Developer ID Installer: ..."`  + `pkgutil --check-signature` verify. LaunchDaemon `mn.sentry.agent.plist` — Label, Program=/usr/local/sentry-agent/run-agent.sh, RunAtLoad=true, KeepAlive{SuccessfulExit=false} (crash restart), ThrottleInterval=10 (hot-loop guard), StandardOut/ErrPath=/var/log/sentry-agent/agent.{log,err}, EnvironmentVariables SENTRY_IMAGE+SENTRY_VERSION placeholders. `run-agent.sh` foreground `exec docker run --init --rm --name sentry-agent --pull always -v config:ro` (launchd owns restart policy нэг загвартай T4-03 Windows Task Scheduler + T4-04 systemd). Missing config.yaml → 60s sleep + exit 2 (launchd hot-loop хаана). `scripts/preinstall` — macOS 12+ gate (`sw_vers -productVersion`), previous LaunchDaemon bootout, stale container rm; `scripts/postinstall` — `install -d -m 0750 -o root -g wheel /etc/sentry-agent`, Docker detect + `brew install --cask docker` fallback console-user-аар (Homebrew байхгүй бол customer-facing install guide log), `docker pull` prefetch, `launchctl bootstrap`/`kickstart`/`enable`. `uninstall.sh` bootout + unlink daemon + `/usr/local/sentry-agent` wipe, `/etc/sentry-agent` хадгалах (reinstall retention). Homebrew cask formula `sentry-agent.rb` tap-д `brew install --cask sentry-agent` ажиллуулна, `depends_on cask: "docker"`, launchctl/pkgutil/delete/zap lifecycle. CI workflow `.github/workflows/agent-installer-macos.yml` — validate job: Homebrew shellcheck -S warning 5 скрипт + `plutil -lint` plist. Build job: macos-latest, cert import random keychain password + `security set-key-partition-list`, productsign conditional `secrets.MACOS_CERT_BASE64`, notarization `xcrun notarytool submit --wait` + `xcrun stapler staple` conditional `secrets.MACOS_NOTARY_KEY_ID` (Apple notary service), artifact upload + tag release-asset. Тест `tests/test_macos_installer_bundle.py` (28 тест): build.sh strict-mode + 3 флаг + pkgbuild+productbuild + version required + sign optional + bash -n; plist `plutil`-free plistlib parse (@SENTRY_IMAGE@ substituted) + Label/Program/RunAtLoad/KeepAlive/ThrottleInterval fields + placeholder presence + log paths; run-agent.sh exec docker run + --init + --rm + config ro + missing-config 60s sleep; preinstall macOS 12 check + bootout; postinstall 0750 ACL + docker detect + brew cask fallback + launchctl bootstrap; uninstall removes daemon + /usr/local/sentry-agent + preserves /etc/sentry-agent; cask depends on docker + launchctl uninstall label; workflow macos-latest runners + shellcheck+plutil validate + sign secret-gated + notary secret-gated + release asset on tag. |
| T4-06 | `GET /api/v1/installer/download` signed URL endpoint | P0 | T4-03, T4-04 | done | `shoplift_detector/app/services/installer_assets.py` — T4-02-ийн HMAC signing төстэй боловч payload schema нь (tenant_id, os, asset_url, expires_at) 3-OS Literal guard-тай. `SUPPORTED_OS = {"linux", "windows", "macos"}` (айдас). `resolve_asset_url(os, base_url, overrides)` default `DEFAULT_ASSET_PATHS` → `/{os}/install.sh|SentryAgentSetup.exe|sentry-agent.pkg`. `issue_download_token` 24h TTL. Config `settings.INSTALLER_BASE_URL = "https://downloads.sentry.mn"` нэмсэн. AUDIT_ACTIONS-д `installer_download_issued` нэмсэн. Endpoint `shoplift_detector/app/api/v1/installer.py`: `GET /installer/download?os=linux|windows|macos` (`CurrentTenant`) — `Literal` Pydantic guard-аар 422 unsupported OS, audit_log entry {action, tenant_id resource, os+asset_url details, IP + user-agent forensics trail}, `PUBLIC_BASE_URL`/request base fallback-аар download URL assemble. `GET /installer/download/{token}` (unauth) — verify_download_token → 302 RedirectResponse to asset CDN, бусад бүх verification failure 410 Gone (enumeration хаалт). Response schema `InstallerDownloadResponse`. Тест `tests/test_installer_download.py` (21 тест): resolve (default paths, trailing-slash normalize, overrides, unsupported raise), token roundtrip + tamper + wrong-secret + expired + malformed + forged OS guard, 24h TTL, endpoint audit-log write exactly once, 3-OS parameterized, Literal 422 on bsd, 302 redirect location verify, 410 on invalid + expired. |
| T4-07 | `POST /api/v1/agents/register` (agent first-start) | P0 | T4-02 | done | Migration `alembic/versions/20260423_02_add_agents.py` — `agents` UUID PK `gen_random_uuid()`, `tenant_id UUID FK → tenants(tenant_id) ON DELETE CASCADE`, `hostname TEXT`, `platform TEXT CHECK IN linux/windows/macos`, `agent_version TEXT`, `registered_at` + `last_heartbeat_at TIMESTAMPTZ`, `metadata JSONB DEFAULT '{}'`, `UNIQUE(tenant_id, hostname)` (idempotent re-register), 2 index (tenant_id btree + last_heartbeat DESC partial), RLS `ENABLE+FORCE` с `tenant_isolation` policy `bypass_tenant='on' OR tenant_id=current_tenant_id::uuid`. Model `shoplift_detector/app/db/models/agent.py`. Repo `shoplift_detector/app/db/repository/agents.py::AgentRepository.register_or_refresh()` — ON CONFLICT UPSERT agent_id stable after restart. Schemas `shoplift_detector/app/schemas/agent.py` (HostnameStr 1-253, `platform: Literal["linux","windows","macos"]`). Endpoint `shoplift_detector/app/api/v1/agents.py::register_agent` (tenant auth) буцаана `{agent_id, heartbeat_interval_s=60, server_time, registered_at, last_heartbeat_at}`. Router wire `/agents` `Agents` tag. Тест доор (T4-08-тэй хамт) 15 тест. |
| T4-08 | Agent heartbeat endpoint `POST /api/v1/agents/{id}/heartbeat` | P0 | T4-07 | done | `AgentRepository.record_heartbeat()` — UPDATE agents SET last_heartbeat_at WHERE agent_id+tenant_id, `rowcount > 0` ownership check (cross-tenant → rowcount=0). Endpoint `POST /agents/{agent_id}/heartbeat` `CurrentTenant` — UUID path param 422 on malformed, rowcount=0 → **404 Not Found** (403 биш — cross-tenant agent_id enumeration хаалт). Response `AgentHeartbeatResponse` echoes `server_time` (clock drift detection) + `next_heartbeat_in_s=60`. Pure helper `derive_status(last_heartbeat_at, now)` — `pending` / `online` (`OFFLINE_THRESHOLD_SECONDS=300` дотор) / `offline`, naive TZ coerce-д UTC-д автомат normalize (legacy row safety). Constants: `HEARTBEAT_INTERVAL_SECONDS=60`, `OFFLINE_THRESHOLD_SECONDS=300`. Тест `tests/test_agents_api.py` (15 тест): migration revision chain + schema + RLS policy, derive_status (pending / online / offline / naive-TZ / threshold constants), register endpoint (agent_id + interval_60s + ISO server_time, idempotent hostname, 422 bad platform), heartbeat endpoint (happy 200 + next_60s, 404 unknown, 422 malformed UUID). |
| T4-09 | ONVIF WS-Discovery probe (agent-side) | P0 | T4-01 | done | `agent/sentry_agent/probe.py` — stdlib-only UDP multicast (239.255.255.250:3702 `IP_MULTICAST_TTL=1` LAN-confined), SOAP Probe envelope targeting `NetworkVideoTransmitter` с per-call randomized `MessageID` uuid, regex `_parse_probe_match` extract XAddrs + Scopes + хугацаат `onvif://.../{name,hardware,location}/value`, `_arp_lookup` `/proc/net/arp` Linux route (Windows/macOS ONVIF Scopes fallback), `_enrich_manufacturer` 2-path resolver — MAC OUI T4-10 catalog lookup эхлээд дараа Scopes hardware-name match, deduplication (ip, xaddrs) pair-аар. 5s timeout default. `ProbeResult` frozen dataclass с `as_dict()` serialization. CLI subcommand `sentry-agent probe --timeout 5 [--json]`. Тест `tests/test_agent_probe.py` (12 тест): parser xaddrs/scopes/extras, malformed empty, multi-xaddrs, socket sendto correct mcast addr + NetworkVideoTransmitter payload, ProbeMatch parse into ProbeResult, non-probematch ignore, dedup, result as_dict, CLI JSON empty + text output. |
| T4-10 | Common RTSP URL patterns DB (Hikvision/Dahua/Axis) | P0 | — | done | `shoplift_detector/app/services/rtsp_patterns.json` — 6 vendor (Hikvision, Dahua, Axis, Uniview, TP-Link, generic fallback), per-vendor `oui_prefixes` MAC OUI массив, `default_port=554`, `credential_hints` factory-default массив (note-тэй), `patterns[].template` `{user}/{password}/{ip}/{port}/{channel}/{stream}` placeholder-тэй + `score` ranking (modern ISAPI Hikvision=100 vs legacy=60). Loader `rtsp_patterns.py` — `load_patterns()` `lru_cache`, `RTSP_PATTERNS_PATH` env override (air-gapped operators замаар). `_normalize_oui` MAC шинэлэхэд hex-only uppercase 6 char (colons/dashes/no-separator бүгдийг дэмжинэ), `match_by_oui` fallback хийхгүй `None` буцаана (UI мэдэгдээрэй); `get_manufacturer`, `candidate_urls(ip, user, password, port?)` score DESC sort-лж URL-ын жагсаалт буцаана, unknown vendor → generic fallback; `credential_hints(id)` T4-14-д зориулсан; `list_manufacturers()` credential_hints-ийг хассан UI-safe list. Тест `tests/test_rtsp_patterns.py` (21 тест): JSON parse + version + 6 vendor presence, OUI match 4 формат (colon/dash/no-sep/case-insensitive), unknown None-короноа, short input None, URL render per-vendor s score ordering, port override, unknown fallback to generic, determinism, hints shape, empty for unknown, list-manufacturers hint strip, env override wins. |
| T4-11 | Camera test connection `POST /api/v1/cameras/test` | P0 | T4-09 | done | `shoplift_detector/app/services/camera_test.py::test_camera()` — structural `VideoCaptureLike` Protocol (injectable for тест без cv2), lazy `cv2.VideoCapture(url, CAP_FFMPEG)` factory, 1-frame decode + `_maybe_shrink` to 640px (aspect-preserving, fault-tolerant на non-ndarray), `cv2.imencode('.jpg', quality=60)` base64-encoded thumbnail, wall-clock FPS estimation через 1.5s window (cap.get(CAP_PROP_FPS) over-reports on bandwidth-capped cameras). `_redact()` strips `user:password` from logs. Failure branches return `CameraTestResult(ok=False, credential_hints=...)` — T4-14 integration via `rtsp_patterns.credential_hints(manufacturer_id)`. Schema `CameraTestRequest` (url 8-2048) + `CameraTestResponse`. Endpoint `POST /api/v1/cameras/test` (`CurrentTenant`) publishes `camera_tested` to onboarding broker. Тест `tests/test_camera_test.py` (10 тест): happy path (thumbnail/fps/latency), closed stream → hints, None-frame graceful fail, unknown manufacturer no hints, capture_factory exception-д crash хийхгүй structured fail, small frame no-shrink, URL password redaction, endpoint happy+failure+422. |
| T4-12 | WebSocket `/api/v1/onboarding/status` | P0 | T4-07 | done | `shoplift_detector/app/services/onboarding_events.py::InMemoryBroker` — per-tenant asyncio pub/sub, 5 event type (`AGENT_REGISTERED/AGENT_HEARTBEAT/CAMERA_DISCOVERED/CAMERA_TESTED/INSTALLER_DOWNLOADED`), `make_event()` standard envelope с ISO `ts`, `_Queue(maxsize=256)` bounded backlog drop-oldest strategy (slow consumer memory хамгаалалт), `_replay` deque(maxlen=32) — late subscriber-т сүүлийн 32 event replay. `Broker` / `Subscription` Protocol-уудыг Redis backend migration path-д pin. Endpoint `shoplift_detector/app/api/v1/onboarding_status.py::onboarding_status_stream` WebSocket — `Authorization: Bearer sk_live_*` header эсвэл `?token=` query fallback (browser `new WebSocket` headers set хийх боломжгүй тохиолдолд), API key hash → tenant match → `status='active'` check, failure close code **4401** (не 1008). Accept → `{type:"hello", tenant_id, server_time}` frame → event forwarding loop → WebSocketDisconnect catch → unsubscribe cleanup. Publisher hooks `agents.py` register+heartbeat, `cameras.py::test_camera_connection`, `installer.py::create_installer_download_url`. Тест `tests/test_onboarding_status_ws.py` (11 тест): make_event known/unknown, EVENT_TYPES frozen; broker publish → matching tenant delivered, cross-tenant isolation (timeout), late-subscriber history replay (3 event), unsubscribe decrements count, bounded backlog under flood; WebSocket endpoint — missing auth 4401, valid token hello frame, publish → forwarded to client. |
| T4-13 | `/connect-cameras` React page | P0 | T4-11 | done | `security-web/src/pages/Onboarding/ConnectCamerasPage.jsx` — 2-column Mongolian UI. Left: илэрсэн камерын жагсаалт, `fetchDiscoveredCameras()` initial fetch + `openStatusStream()` WebSocket subscriber exponential backoff reconnect (1s → 30s cap), `camera_discovered` event дээр append ашиглан dedup ip-ээр. Right: гараар RTSP URL form, T4-10-ийн 6 vendor select (`Hikvision/Dahua/Axis/Uniview/TP-Link/Generic`). `runTest()` `postCameraTest` call (/api/v1/cameras/test), success bannerOk=true thumbnail_b64 preview (data:image/jpeg;base64) + FPS badge, `CAMERA_CONNECTED` analytics trackEvent. App.jsx route `/connect-cameras` wire. ReadyPage checklist-ын `connect_cameras` алхам энэ хуудас руу шилжүүлнэ. |
| T4-14 | Manufacturer credential hints (Hikvision: admin/12345) | P1 | T4-13 | done | Backend талд `rtsp_patterns.credential_hints(manufacturer_id)` function (T4-10) + `camera_test.py::_hints_for` failure-д surface хийж `CameraTestResponse.credential_hints` буцаана. Frontend талд `ConnectCamerasPage::hintsBlock` memoized — failure bannerOk=false үед `{username / password / note}` triplet-аар monospace amber list харуулна. Default `(хоосон)` displayed for empty-password Axis-like. Manufacturer select-ээр failure-ийн context-д суулгасан vendor-ын hints-г трек хийнэ. Тест `tests/test_camera_test.py::test_closed_stream_reports_failure_with_hints` Hikvision factory-default `admin/12345` surface хийхийг баталгаажуулна. |

### WS-5: Alerting v1.1 (FR-7)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T5-01 | 4-level classifier (GREEN/YELLOW/ORANGE/RED) | P0 | — | done | `shoplift_detector/app/core/severity.py` — frozen `SeverityThresholds` dataclass (default 40/70/85), `__post_init__` strictly-increasing + non-negative guard, `classify()` returns `green|yellow|orange|red` Literal. `NOTIFY_SEVERITIES={yellow,orange,red}` — green-д alert хийхгүй. Store-level override `SeverityThresholdsSchema` (Pydantic `model_validator` ordering guard, `classify()` delegate) `StoreSettings.severity_thresholds` талбарт суулгасан. `ai_service.ShopliftDetector._get_severity_thresholds(store_id)` — auto_learner cache-ээс уншина, dict→domain coerce fail бол `DEFAULT_SEVERITY_THRESHOLDS`-т fallback; inference loop-д single `score >= effective_threshold` check-ыг `severity in NOTIFY_SEVERITIES`-р орлуулсан, bounding-box BGR color нь severity-д мөрдөгдөнө (`red=(0,0,255)`, `orange=(0,128,255)`, `yellow=(0,215,255)`, `green=(0,255,0)`). `_dispatch_alert`/`insert_alert`/`_send_telegram_alert` бүгдэд `severity: SeverityLevel` гүйлгэдэг болгосон. Тест `tests/test_severity_classifier.py` (17) — default 40/70/85 + boundary inclusivity (39.9=green, 40.0=yellow, 69.9=yellow, 70.0=orange, 84.9=orange, 85.0=red, 999=red), non-increasing ValueError, negative ValueError, StoreSettings round-trip + unknown-key reject, `resolve_settings` backfills severity defaults on legacy rows. `tests/test_store_settings.py` expected key set-д `severity_thresholds` нэмсэн. |
| T5-02 | Alert severity column migration | P0 | T5-01 | done | Migration `alembic/versions/20260424_01_add_alert_severity.py` — `ALTER TABLE alerts ADD COLUMN IF NOT EXISTS severity VARCHAR(16) NOT NULL DEFAULT 'green'` + CHECK constraint `alerts_severity_valid (severity IN ('green','yellow','orange','red'))`. Idempotent backfill: `WHEN confidence_score IS NULL → green`, `>=85 → red`, `>=70 → orange`, `>=40 → yellow`, `ELSE green`; `WHERE severity = 'green'` guard нь дахин ажиллуулахад өөрчлөгдсөн rows-ыг давхардахаас сэргийлнэ. Partial index `ix_alerts_severity_nongreen WHERE severity <> 'green'` hot-path dashboard query-д зориулсан. `downgrade()` inverse order DROP INDEX → DROP CONSTRAINT → DROP COLUMN бүх IF EXISTS-тэй atomicity note-той. `shoplift_detector/app/db/repository/alerts.py::insert_alert()` column-discovery pipeline_values-д `severity` key нэмсэн — pre-migration schema-д silently skip хийнэ. Тест `tests/test_alert_severity_migration.py` (6) — revision chain (20260424_01 → 20260423_02), column + NOT NULL + default + CHECK + partial index + backfill thresholds + downgrade order. |
| T5-03 | Telegram bot command handler | P0 | — | done | `shoplift_detector/app/services/telegram_bot.py` — 5 slash команд (`/start`, `/help`, `/status`, `/today`, `/alerts`), `_parse_command` нь `/cmd@BotName args` форматыг ойлгож, per-handler AsyncSessionLocal ашиглан DB lookup тус бүр isolated. Python-telegram-bot dep-ийг оруулалгүй одоогийн httpx-pattern-г (telegram_notifier) үргэлжлүүлсэн — 20MB+ bot framework dep-ийг нэмэхгүйгээр. `telegram_notifier.send_message()` нэмсэн — plain text reply-д сервер-гүй fail хамгаалалт (user blocked / rate-limited тохиолдолд log-аар мэдэгдээд pipeline үргэлжлэнэ). `handle_update(update)` нь `message`/`edited_message` extract, non-command plain text ignore, unknown command → `/help` тайлбар, handler exception → `"Серверийн алдаа гарлаа"` generic reply + `logger.exception`. Endpoint `POST /api/v1/telegram/webhook` — unauth (public) боловч `X-Telegram-Bot-Api-Secret-Token` header-ийг `TELEGRAM_WEBHOOK_SECRET` env-тэй тулгаж invalid → 401, valid → 204 No Content. `.env.example`-д `TELEGRAM_WEBHOOK_SECRET=` + `secrets.token_urlsafe(32)` дээжтэй документ. Тест `tests/test_telegram_bot_dispatcher.py` (14) — `_parse_command` 8 parametrized case (bot-suffix strip, args, whitespace), `/start`/`/help`/unknown реплика, non-message/plain-text ignore, handler exception surfacing, webhook secret enforcement (wrong 401 / correct 204 / non-JSON 400). |
| T5-04 | Multiple manager subscribe per store | P0 | T5-03 | done | Migration `alembic/versions/20260424_02_add_telegram_subscribers.py` — `store_telegram_subscribers` SERIAL PK, `store_id INTEGER NOT NULL REFERENCES stores(id) ON DELETE CASCADE`, `chat_id TEXT NOT NULL`, `role TEXT NOT NULL DEFAULT 'manager'`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`; CHECK constraint `role IN ('owner', 'manager', 'staff')` + UNIQUE `(store_id, chat_id)` — idempotent subscribe. 2 индекс (store_id + chat_id хоёр чиглэлийн lookup-д). Backfill `INSERT INTO store_telegram_subscribers SELECT id, telegram_chat_id, 'owner' FROM stores WHERE telegram_chat_id IS NOT NULL AND TRIM <> '' ON CONFLICT DO NOTHING` — legacy singleton-ыг owner role-оор автоматаар шилжүүлнэ; `stores.telegram_chat_id` column нь T5-07-д тусдаа миграцаар устгах хүртэл fallback хэвээр үлдэнэ. Model `shoplift_detector/app/db/models/telegram_subscriber.py::StoreTelegramSubscriber`. Repo `shoplift_detector/app/db/repository/telegram_subscribers.py::TelegramSubscriberRepository` — `_exists()` information_schema probe-аар pre-migration tolerant (`list_for_store` / `add` / `remove` / `find_by_chat` бүгд `[]`/`None` буцаана), `add()` ON CONFLICT UPDATE role (role-promotion), `ALLOWED_ROLES = {owner,manager,staff}` bad-role ValueError. API `app/api/v1/telegram.py`: `GET /subscribers/{store_id}` (жагсаах), `POST /subscribers` (нэмэх/role шинэчлэх), `DELETE /subscribers` — бүгд `require_store_access` + tenant scope 404-on-cross-tenant. `ai_service._send_telegram_alert()` нь subscriber list-аар fan-out хийдэг — subscribers байхгүй бол `stores.telegram_chat_id` singleton fallback; per-chat failure wrap нь нэг муу chat_id бусдыг блоклохоос сэргийлнэ. Тест `tests/test_telegram_subscribers_migration.py` (6) — revision chain 20260424_02 ← 20260424_01, column shape + FK cascade + role CHECK + UNIQUE + backfill `'owner'` + `ON CONFLICT DO NOTHING` + lookup indexes хоёулаа. |
| T5-05 | Inline acknowledge button on alert | P0 | T5-03 | done | Migration `alembic/versions/20260424_03_add_alert_acknowledged_at.py` — `alerts.acknowledged_at TIMESTAMPTZ NULL` (NULL = unacknowledged, dashboard-ын default filter contract) + `alerts.acknowledged_by_chat_id TEXT NULL` (loose TEXT — subscriber мөр устсан ч audit trail хадгалагдана), partial index `ix_alerts_unacknowledged_event_time ON alerts(event_time DESC) WHERE acknowledged_at IS NULL` (open-alert query-ын hot path). `shoplift_detector/app/db/models/alert.py::Alert`-д `acknowledged_at` + `acknowledged_by_chat_id` ORM mapped. `shoplift_detector/app/db/repository/alerts.py::AlertRepository.mark_acknowledged(alert_id, chat_id)` — `UPDATE alerts SET acknowledged_at = now(), acknowledged_by_chat_id = :chat_id WHERE id = :id AND acknowledged_at IS NULL` idempotent (double-tap нь original timestamp-ыг дарж бичихгүй, rowcount > 0 буцаана), pre-migration schema-д `False` fallback; `get_ack_state(alert_id)` — callback reply-д "already ack'd by X" render-ын 2-талбайн lightweight read. `shoplift_detector/app/services/telegram_notifier.py::build_ack_keyboard(alert_id)` — inline_keyboard: `[[{text: "✅ Анхааралаа авлаа", callback_data: "ack:{alert_id}"}]]`, `ACK_CALLBACK_PREFIX="ack:"` 10-орон alert_id-ын хувьд callback_data 64-байт cap-аас ч хэтрэхгүй. `send_alert(..., alert_id=None)` — `alert_id` supplied бол `reply_markup` JSON-string (form-data) эсвэл native dict (JSON body) болгон `sendPhoto`/`sendMessage` дамжуулна. Шинэ methods: `answer_callback_query(callback_query_id, text=..., show_alert=...)` (Telegram spinner цэвэрлэж 200-char cap enforce хийнэ) + `edit_message_reply_markup(chat_id, message_id, reply_markup=None)` (товч хасах — двойное нажатие боломжгүй). `shoplift_detector/app/services/telegram_bot.py::handle_update` — `update["callback_query"]` эхний branch, `_handle_callback_query` нь `ack:{id}` prefix+int parse, unknown prefix → generic "Ойлгомжгүй товч" answer (spinner clear, DB mutation байхгүй), malformed id → "буруу формат", happy path → repo.mark_acknowledged → "✅ Анхаарал авлаа", prior-ack → "Аль хэдийн авсан ({owner})", finally `edit_message_reply_markup(reply_markup=None)` — message-ийн товчийг хасна. `ai_service._dispatch_alert` нь `insert_alert`-ын буцаасан `alert_id`-г `_send_telegram_alert`-т дамжуулдаг болсон — subscriber fan-out бүрт inline товч идэвхтэй. Тестүүд: `tests/test_alert_acknowledged_migration.py` (4) — revision chain + nullable columns + partial index order + downgrade reverse; `tests/test_alert_ack_callback.py` (9) — keyboard shape + 64-byte cap + happy-path flip + second-click prior-owner + unknown prefix no-mutation + malformed id + answerCallbackQuery HTTP + editMessageReplyMarkup payload shape. |
| T5-06 | Email alerts (Resend) | P0 | T2-02 | done | `shoplift_detector/app/services/alert_copy.py::build_email_subject` + `build_email_bodies(language='mn'\|'en')` — pure template builders (text + HTML), mn/en toggle per call, inline snapshot via `snapshot_cid` (CID reference for Resend/MIME attachment); CID-гүй үед `<img>` тагийг орхино. `escalation_dispatcher._build_email_sender()` — `RESEND_API_KEY` тохируулагдсан бол `ResendEmailSender` (T2-02 Protocol), үгүй бол `RecordingEmailSender` dev/test-д. Fan-out `StoreSettings.notification_channels.email.addresses` дээр; address тус бүр per-recipient error isolated — нэг Resend-ийн 4xx нь бусад хүлээн авагчдыг блоклохгүй. Severity gate: yellow+ (green хийхгүй). Делверийн бичлэг `alert_escalations` table-д `(channel='email', recipient=addr)` record болно. Тест `tests/test_alert_copy.py::test_email_subject_has_store_name_and_severity` / `test_email_bodies_include_context_and_disclaimer_mn` / `test_email_inline_snapshot_cid_when_supplied` / `test_email_html_omits_image_when_no_cid` + dispatcher integration `test_escalation_dispatcher.py::test_yellow_dispatches_telegram_and_email_only`. `.env.example`-д `RESEND_API_KEY` + `EMAIL_FROM` документлэсэн. |
| T5-07 | FCM push notifications (ORANGE/RED) | P0 | — | done | Migration `alembic/versions/20260424_05_add_push_tokens.py` — `push_tokens(id, user_id FK users(id) CASCADE, token TEXT UNIQUE, platform TEXT CHECK(ios/android/web), created_at, last_seen_at)`. Model `shoplift_detector/app/db/models/push_token.py` + repo `shoplift_detector/app/db/repository/push_tokens.py` (`register()` ON CONFLICT UPDATE `last_seen_at`+`platform` upsert, `unregister(token)`, `list_for_store(store_id)` — users-аар дамжуулж org_id-ээр join хийнэ; бүгд pre-migration tolerant — `[]`/`None` буцаана). Sender `shoplift_detector/app/services/fcm_sender.py::LegacyFcmSender` — `FCM_SERVER_KEY` дээр суурилсан legacy HTTP `send` endpoint (deprecated v1-рүү шилжих upgrade-path нь `google-auth` нэмэх үед), 5xx → `FcmUnavailableError` fallback signal, Protocol-тай тул `RecordingFcmSender` dev-ийн recorder. Dispatch only when severity ∈ {orange, red} — mobile-first policy per spec. Payload `alert_copy.build_fcm_payload(notification+data)`, recipient field-д `token[:16]` л хадгалдаг (audit log дотор full token PII gate). Endpoints `shoplift_detector/app/api/v1/push.py`: `POST /api/v1/push/tokens` (CurrentUser, `Literal["ios","android","web"]` platform validator), `DELETE /api/v1/push/tokens/{token}` (path-based — DELETE body нь HTTP anti-pattern, proxy strip боломжтой). Pre-migration → 503 so the mobile client shows a clear "сервер бэлэн болоогүй" state rather than silently "success". Тест `tests/test_push_tokens_migration.py` (3) + `tests/test_push_token_endpoint.py` (6) — auth 401, platform 422, 503 pre-migration, happy register + revoke, 404 on unknown token + `tests/test_escalation_dispatcher.py::test_orange_adds_fcm` integration. `.env.example`-д `FCM_SERVER_KEY=` документлэсэн. |
| T5-08 | Twilio SMS (RED only) | P1 | — | done | `shoplift_detector/app/services/alert_copy.py::build_sms_body()` — Mongolian template, GSM-7 160-char single-segment guard (Twilio per-segment billing-ээс хамгаална, тест `test_sms_body_single_segment` 20-char store name-тай хэтрэхийг шалгана). `escalation_dispatcher.dispatch_alert` нь severity=='red' үед `StoreSettings.notification_channels.sms.numbers` дээр fan-out хийнэ (orange/yellow-ийн хувьд skip). Rate-limit module-level cache `_SMS_LAST_SENT: dict[(tenant_id, phone), monotonic]` + `_sms_rate_limited(tenant_id, phone)` pure function — 300s window, side-effect updates timestamp when NOT limited (true caller path). Blocked call → `alert_escalations.failed_at` + `error="rate_limited: 1/5min/chat"` бичнэ (audit trail-д reconstruct хийж болохуйц). `TwilioSmsSender` нь аль хэдийн T2-03-д суусан (5xx → `SmsUnavailableError`, 4xx → RuntimeError); dispatcher `SmsUnavailableError` catch хийж failure row-д `error="provider_outage: {exc}"` prefix-тэй бичнэ. Тест `tests/test_escalation_dispatcher.py::test_sms_rate_limit_*` (4 pure-logic — first-call-ok, second-call-blocked, different-phone-independent, different-tenant-independent) + `test_red_adds_sms` + `test_red_sms_rate_limited_second_alert_same_tenant` (integration). |
| T5-09 | Escalation log per alert | P0 | T5-05 | done | Migration `alembic/versions/20260424_04_add_alert_escalations.py` — `alert_escalations(id SERIAL, alert_id INTEGER NOT NULL, channel TEXT NOT NULL, recipient TEXT, delivered_at TIMESTAMPTZ, failed_at TIMESTAMPTZ, error TEXT, acknowledged_by TEXT, created_at TIMESTAMPTZ DEFAULT now())`. **Soft FK** (no `REFERENCES alerts` constraint) нь T2-23 retention sweeper-т `alerts` устгагдах үед escalation audit trail-ийг амьд үлдээхийн тулд — compliance requirement. `CHECK alert_escalations_channel_valid (channel IN ('telegram', 'email', 'fcm', 'sms'))`. `CHECK alert_escalations_outcome_coherent ((delivered IS NOT NULL AND failed IS NULL) OR (delivered IS NULL AND failed IS NOT NULL) OR both NULL)` — нэг row хоёр outcome-тай байхгүй болгох guard (dashboard аль нэг timestamp-д найдаж болно). Indexes: `ix_alert_escalations_alert (alert_id, created_at DESC)` hot-path customer-portal query, `ix_alert_escalations_channel_recent (channel, created_at DESC)` SRE "recent failures on channel X" query. Model `shoplift_detector/app/db/models/alert_escalation.py` + repo `shoplift_detector/app/db/repository/alert_escalations.py` (`log_delivery`, `log_failure` — 1000-char error truncate + ALLOWED_CHANNELS validation, `list_for_alert` ordered newest-first, бүгд pre-migration tolerant). Dispatcher `shoplift_detector/app/services/escalation_dispatcher.py` нь 4 сувгийн fan-out + per-channel failure isolation + per-recipient `log_delivery`/`log_failure` логоор нэгтгэсэн single entry point — `ai_service._dispatch_alert` одоо `dispatch_alert(AlertContext(...))`-ыг дуудаж байна (өмнөх direct `_send_telegram_alert` call-ыг орлуулсан). Customer portal endpoint `GET /api/v1/alerts/{alert_id}/escalations` — `AlertRepository.get_by_id`-аар ownership шалгана, non-super_admin + cross-tenant → 404 (403 биш — enumeration хаалт), super_admin аль ч tenant хардаг. Тест `tests/test_alert_escalations_migration.py` (6) — chain, columns, channel CHECK, outcome-coherency CHECK, **no FOREIGN KEY to alerts** (soft-FK invariant), lookup indexes; `tests/test_alert_escalations_endpoint.py` (5) — 401 no auth, 404 missing alert, 404 cross-tenant, happy-path returns rows, super_admin bypass; `tests/test_escalation_dispatcher.py` (9) — severity gates yellow/orange/red, green short-circuit, email failure не блоклох other channels. `tests/test_alert_pipeline_dedup.py` regression updates — патч нь `_send_telegram_alert` оронд `app.services.escalation_dispatcher.dispatch_alert` руу шилжсэн, AlertContext.alert_id нь insert_alert-ийн буцаасантай тохирч байгааг assert хийнэ. |
| T5-10 | Store alert threshold customization | P1 | T5-01 | done | Backend — T5-01-ээс `StoreSettings.severity_thresholds` (`SeverityThresholdsSchema` yellow/orange/red, `model_validator` strictly-increasing guard) болон `StoreSettingsPatch.severity_thresholds` аль хэдийн байсан; Frontend `security-web/src/components/Settings/SeverityThresholdsSliders.jsx` (шинэ) — 3 `<input type="range" min=0 max=100>` slider (yellow/orange/red), `MIN_GAP=1` invariant enforcement местний дэд тал (yellow нь orange-аас багадаа 1-ээр бага байх гэх мэт) — drag-аар cross хийхэд автоматаар neighbor-оос 1 нэгж зөрүүтэй pin хийгдэнэ; backend `model_validator` нь гараар API call хийсэн malformed payload-ыг дахин шалгана. Tailwind progress bar ( slider per tier), Mongolian нэршил ("хянаж байна" / "анхаарах хэрэгтэй" / "яаралтай" T5-11 wording policy-тэй нийцсэн). `onSave(value)` prop-ыг дуудах — caller үүнийг `PATCH /api/v1/stores/{id}/settings`-д шилжүүлэх (existing T2-18 endpoint `StoreSettingsPatch`-ыг ашигладаг). Error banner rose-400 бүртгэл backend refuse-ыг харуулна. `saving` prop нь loading state disable хийж `Хадгалж байна…` text свопнох. T5-01 `tests/test_severity_classifier.py::test_store_settings_rejects_non_increasing_thresholds` нь backend invariant-ийг гараар (UI-аас ирсэн эсвэл API-оос) холимог rejectdo хийдгийг батална. |
| T5-11 | Alert suppression (UI guidelines compliance) | P0 | — | done | `shoplift_detector/app/services/alert_copy.py::BANNED_PHRASES = ("Хулгайч", "Гэмт хэрэгтэн", "criminal", "thief")` — frozen tuple, алдаан вариантууд хоорондоо case-insensitive match. `severity_header(severity, language)` нь "Яаралтай: анхаарах хэрэгтэй" / "Анхаарах хэрэгтэй" / "Хянаж байна" (mn) болон "Urgent: review required" / "Review recommended" / "Watching" (en) буцаана — "хулгайч" / "гэмт хэрэгтэн" хэзээ ч харагдахгүй. `DISCLAIMER_MN` / `DISCLAIMER_EN` legal disclaimer нь email template-ийн HTML + plain-text бүгдэд present байдаг (`build_email_bodies`-д header + body + footer хоёр газарт `AI нь зөвхөн сэжигтэй зан үйлийг тэмдэглэдэг бөгөөд хүний шалгалт шаардлагатай. Алдаа гарах магадлалтай.` хэвлэгдэнэ). Lint test `tests/test_alert_copy.py::test_no_builder_output_contains_banned_wording` — parametrize хийсэн `BANNED_PHRASES` тус бүрийн хувьд `_all_copy_outputs()` generator бүх severity × language × channel комбинацийг ухраан нэгтгэж assert `lower() not in output.lower()` — шинэ банн үг нэмэхэд тест файл засах шаардлагагүй (source of truth: `alert_copy.BANNED_PHRASES` tuple). `test_disclaimer_present_in_both_languages` — mn + en двух хэл дээр текст + HTML body-д disclaimer байгаа эсэхийг шалгадаг. Telegram (T5-03 `telegram_notifier._severity_header`) нь "Сэжигтэй үйлдэл" / "Анхаар: хянаж байна" / "Яаралтай: хулгайн магадлал өндөр" — T5-11 дэх "never show 'хулгайч'" policy-г амжилттай мөрдөнө (cf. banned-phrase lint тест энэ модулиар ч дамжин шалгах боломжтой). |

### WS-6: Observability & status

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T6-01 | Status page at `status.sentry.mn` | P1 | — | todo | Better Stack / Statuspage.io. Uptime monitor against `/health`. Public. |
| T6-02 | Per-tenant Prometheus labels | P1 | T1-01 | todo | `tenant_id` label on all custom metrics (fps, latency, alert_count). |
| T6-03 | Per-tenant GPU usage alert | P1 | T6-02 | todo | Prometheus rule: per-tenant gpu_seconds > 80% of daily quota → PagerDuty. |
| T6-04 | Sentry SDK tenant context | P1 | T1-05 | todo | `sentry_sdk.set_tag("tenant_id", tenant.id)` in middleware. |
| T6-05 | Cross-tenant query detector (Loki rule) | P0 | T1-14 | todo | Loki regex alert on log `cross_tenant_detected=true`. Critical severity. |
| T6-06 | Onboarding funnel Grafana dashboard | P1 | T2-09 | todo | PostHog → Grafana connector. Widget per step. |

### WS-7: Identity & 2FA (Phase 1 subset)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T7-01 | Existing JWT → extended with `tenant_id` + role | P0 | T1-01 | done | `shoplift_detector/app/core/security.py::create_access_token` — `iat` integer timestamp auto-stamp (caller `iat` setdefault-ээр хамгаалагдсан), `_decode_token` `tenant_id` + `iat` буцаана. Payload одоо `{sub, role, org_id, user_id, tenant_id, iat, exp}`. Backward-compat: хуучин `tenant_id`-гүй token `None`-той амжилттай decode. Resolver `TenantRepository.get_tenant_id_for_organization(org_id)` — `organization_tenant_map` дээгүүр `scalar_one_or_none`, `None` org_id short-circuit (DB round-trip алгасна). Token issuance цэгүүд: `shoplift_detector/app/api/v1/auth.py::login` + `shoplift_detector/main.py::legacy_login` хоёулаа `TenantRepository`-гоор `tenant_id` lookup хийж payload-д хавсаргана. Legacy `app/api/api.py` + `app/api/auth.py` cut — main.py-д wire хийгдээгүй (`app.include_router(api_router)` v1 зөвхөн include). Тест `tests/test_security.py::TestJWTTokens` (4 шинэ тест — iat stamp, tenant_id roundtrip, legacy token без tenant_id, caller-supplied iat preserved) + `tests/test_tenant_auth.py` (3 шинэ тест — resolver returns string UUID / None / short-circuit). |
| T7-02 | Role-based access (Owner/Manager/Viewer/Billing) | P0 | T7-01 | todo | `users.role` + FastAPI dependency `require_role(...)`. |
| T7-03 | 2FA TOTP setup (Manager+) | P1 | T7-01 | todo | `pyotp` + QR code. Mandatory for manager & above. |
| T7-04 | Login rate limiting per IP + email | P0 | — | todo | 5 attempts / 15 min. 429 + lockout. |

---

## Phase 2 — Growth (Months 4–9)

### WS-8: Cross-camera Re-ID (FR-3)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T8-01 | OSNet embedding extractor (inference worker) | P1 | — | done | 512-dim vector per detected person. Latency <100ms. |
| T8-02 | Qdrant per-tenant collection manager | P1 | T1-01 | todo | `reid_tenant_{uuid}` collection auto-create. Drop on tenant delete. |
| T8-03 | Similarity matching (cosine >0.75, 5s window) | P1 | T8-01, T8-02 | todo | Return: existing_person_id OR new. Action log merge. |
| T8-04 | Redis person state (30-min expire) | P1 | T1-12 | todo | Key: `tenant:{uuid}:store:{id}:person:{pid}`. Hash: action history, score, last_seen. |
| T8-05 | Person ID format: `P-{store}-{YYYYMMDD}-{seq}` | P1 | T8-03 | todo | Daily sequence reset. Cross-camera stable. |
| T8-06 | Gait + height fallback (P1 cases only) | P2 | T8-03 | todo | Winter clothing use case. Custom similarity weighting. |

### WS-9: VLM verification (FR-4)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T9-01 | Qwen2.5-VL 7B on transformers (vLLM optional) | P1 | — | done | `app/services/vlm_service.py` — lazy load, async semaphore-guarded inference, hard timeout. vLLM/4-bit quant deferred to GPU deploy task. |
| T9-02 | VLM action description generator | P1 | T9-01 | done | `vlm_service.describe_alert()` returns caption + confidence + structured JSON evidence. Prompt template ready for Mongolian extension. |
| T9-03 | RAG suppression pipeline | P1 | T9-02 | done | `app/services/rag_retriever.py` (pgvector `rag_corpus.embedding` column, multilingual-e5 embeddings) + `app/services/rag_vlm_pipeline.py` orchestrator. Per-store known_fp matching with `rag_fp_threshold`. Migration `20260427_02` adds the extension + HNSW cosine index. |
| T9-04 | `rag_decision` + `vlm_decision` writers | P1 | T9-03 | done | `ai_service._dispatch_alert` calls `rag_vlm_pipeline.evaluate()` and passes verdicts straight to `AlertRepository.insert_alert`. Suppressed alerts persist but skip escalation. |
| T9-05 | Suppression dashboard (per-store suppression rate) | P2 | T9-04 | todo | Grafana panel. Alert when rate >90% (potential over-suppression). |
| T9-06 | RAG corpus admin API + frontend | P1 | T9-03 | done | `POST/GET/DELETE /api/v1/stores/{id}/rag-corpus` + `RagVlmSettings.jsx` + `AlertVlmDetail.jsx` + `AlertVerdictBadges.jsx`. Migration `20260427_01` adds `rag_corpus` and `vlm_annotations` tables. |
| T9-07 | GPU image + Qdrant compose service | P1 | T9-01 | done | `Dockerfile.gpu` (CUDA 12.1 base) and `docker-compose.yml` Qdrant service shipped. Live GPU deploy tracked separately under ops. |

### WS-10: Mobile app (FR-6, FR-8)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T10-01 | React Native codebase скаффолд | P2 | — | todo | Expo + TypeScript. Shared API client with web. |
| T10-02 | Live camera grid screen | P2 | T10-01 | todo | 1/4/9/16 view. WebRTC or MJPEG fallback. |
| T10-03 | Alert queue screen | P2 | T10-01 | todo | Sorted by severity + time. Filter toggle. |
| T10-04 | Push notification handler | P2 | T10-01, T5-07 | todo | FCM / APNs. Tap → open alert detail. |
| T10-05 | Offline queue (acknowledge later) | P2 | T10-01 | todo | Redux-persist. Retry on reconnect. |
| T10-06 | iOS App Store + Google Play submission | P2 | T10-01 | todo | Apple Dev + Google Play accounts. Review process. |

### WS-11: Live streaming v2 (FR-6.1)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T11-01 | MediaMTX ingest deploy | P2 | — | todo | Docker container. RTSP → WebRTC converter. |
| T11-02 | WebRTC signaling server | P2 | T11-01 | todo | Per-tenant room. Peer-to-peer when possible. |
| T11-03 | TURN server fallback | P2 | T11-02 | todo | coturn deploy. NAT traversal for restrictive networks. |
| T11-04 | Sub-stream selector (640x480 for grid) | P2 | T11-01 | todo | ONVIF sub-stream profile auto-detect. |
| T11-05 | Web player component (WebRTC consumer) | P2 | T11-02 | todo | Replace `<img>` MJPEG tag. Latency metric <2s. |

### WS-12: Customer portal (FR-14)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T12-01 | Customer portal sidebar nav | P1 | T7-02 | todo | Dashboard / Cameras / Alerts / Team / Billing / Settings. Mobile responsive. |
| T12-02 | Team member invite + role assignment | P1 | T7-02 | todo | Email invite flow. Role: Owner/Manager/Viewer/Billing. |
| T12-03 | Billing history + invoice PDF download | P1 | T3-06 | todo | Table: date, amount, status, PDF link. |
| T12-04 | Payment method update UI | P1 | T3-04 | todo | Stripe Elements. QPay bank account update. |
| T12-05 | Usage stats + quota gauge | P1 | T3-05 | todo | Progress bars: cameras used / max, GPU sec / max, storage used / max. |
| T12-06 | 2FA setup UI | P1 | T7-03 | todo | QR code display. Backup codes generate. |
| T12-07 | API key management UI | P1 | T1-06 | todo | List keys, last used, created. Rotate button. |
| T12-08 | Audit log viewer | P1 | — | todo | Filter by user, action, date. Export CSV. |

### WS-13: Retraining pipeline (FR-10)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T13-01 | Weekly feedback export cron | P2 | — | todo | Anonymize (no face embeddings). Export to training bucket. |
| T13-02 | Per-store threshold/weight fine-tune (extend auto-learner) | P2 | T13-01 | todo | Already exists — extend to use 200+ feedback instead of 20+. |
| T13-03 | YOLO11-pose fine-tune pipeline | P2 | T13-01 | todo | Monthly cadence. Mongolian dataset. |
| T13-04 | A/B test new weights (10% traffic) | P2 | T13-03 | todo | Feature flag `ai.weights_version_candidate`. Statsig or custom. |
| T13-05 | Rollback automation on precision drop | P2 | T13-04 | todo | Metric threshold. Auto-revert on regression. |

---

## Phase 3 — Scale (Months 10–12)

### WS-14: Enterprise tier

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T14-01 | Dedicated GPU tenant flag | P3 | T1-01 | todo | `tenants.dedicated_gpu BOOLEAN`. GPU pool scheduler respects. |
| T14-02 | SSO/SAML integration (Okta, Azure AD) | P3 | T7-01 | todo | python-saml / authlib. Per-tenant IdP config. |
| T14-03 | Custom action weight fine-tune UI | P3 | T13-02 | todo | Enterprise-only. Slider UI in customer portal. |
| T14-04 | Custom DPA contract PDF template | P3 | — | todo | Per-tenant DPA generator. Admin upload additional terms. |
| T14-05 | 24/7 phone support rotation | P3 | — | todo | OpsGenie / PagerDuty schedule. Enterprise tier only. |
| T14-06 | SLA contract + uptime credit policy | P3 | T6-01 | todo | 99.5% SLA. Monthly uptime report with credit calculation. |

### WS-15: Scale infrastructure

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T15-01 | PostgreSQL sharding by tenant_id | P3 | T1-01 | todo | Citus / PgBouncer route. Migration plan with zero-downtime. |
| T15-02 | Multi-region deploy (Korea fallback) | P3 | T15-01 | todo | Active-passive. DNS failover. Data replication. |
| T15-03 | GPU pool auto-scaler | P3 | — | todo | Inference queue depth >10 for 5min → provision new GPU. |
| T15-04 | Read replica for analytics | P3 | — | todo | Streaming replication. Grafana/Metabase connect to replica. |
| T15-05 | S3 Glacier archive policy | P3 | — | todo | Clips >90 days → Glacier. Retrieval on-demand. |

### WS-16: Compliance & audit

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T16-01 | SOC 2 Type 1 gap analysis | P3 | — | todo | Vendor (Vanta / Drata). Control matrix fill. |
| T16-02 | External penetration test | P3 | T1-15 | todo | Annual vendor pen test. Remediation SLA. |
| T16-03 | Mongolian privacy law full compliance audit | P3 | — | todo | Legal review. DPA template finalize. |
| T16-04 | Right-to-be-forgotten automation | P3 | T1-11 | todo | Privacy email → ticket → 30-day SLA purge. |
| T16-05 | Customer data export tool | P3 | T3-10 | todo | Full tenant data export (JSON + video clips). MinIO signed URL. |

### WS-17: Analytics & insights (Phase 2 overflow)

| ID | Title | Priority | Depends on | Status | DoD |
|---|---|---|---|---|---|
| T17-01 | Customer flow heatmap per store | P2 | T8-01 | todo | Aggregate person trajectories. Grafana heatmap panel. |
| T17-02 | Peak hour analysis | P2 | — | todo | Weekly Mongolian report. Auto-email. |
| T17-03 | Weekly auto-report (Mongolian + English) | P2 | T5-06 | todo | Jinja2 template + Resend. Visitor count, alerts, suppression. |

---

## Cross-cutting (all phases)

| ID | Title | Priority | Status | DoD |
|---|---|---|---|---|
| TX-01 | Migrate all legacy `/token`, `/alerts`, `/video_feed` endpoints to `/api/v1/*` | P1 | wip | Deprecation headers already set (T02-21). Sunset date 2026-07-01. |
| TX-02 | Remove `organization_id` from public API | P2 | blocked | Depends on T1-02. After migration complete. |
| TX-03 | Localization: Russian + English toggle | P2 | todo | next-i18next or similar. UI-only, alerts stay Mongolian default. |
| TX-04 | Documentation: API reference (OpenAPI) | P1 | wip | FastAPI auto-gen. Host at `docs.sentry.mn`. Mongolian descriptions. |
| TX-05 | Developer sandbox environment | P2 | todo | `sandbox.sentry.mn`. Synthetic data. Free API keys for integration testing. |

---

## Recommended execution order

```
Month 1
├─ W1-W2   T1-01, T1-02, T1-03           (tenant UUID schema)
├─ W2-W3   T1-05, T1-06, T7-01           (API key + JWT extend)
└─ W4      T2-01, T2-02, T2-05           (signup + OTP + page)

Month 2
├─ W5      T3-01, T3-02                   (QPay checkout)
├─ W6      T3-03, T3-04, T2-07            (Stripe + trial)
├─ W7      T4-01, T4-02, T4-06            (Docker agent)
└─ W8      T4-09, T4-11, T4-13            (ONVIF + camera test)

Month 3
├─ W9      T5-01, T5-02, T5-03            (4-level + bot cmds)
├─ W10     T5-05, T5-07, T5-09            (inline btn + FCM + escalation)
├─ W11     T6-01, T2-09, T1-15            (status page + PostHog + pen test)
└─ W12     Pilot go-live + hotfix buffer
```

---

## Related

- `06_Sentry_MVP_Roadmap.md` — phase goals, workstreams, acceptance criteria
- `01_Sentry_PRD_v1.1.md` — functional/non-functional requirements
- `05_Sentry_Multi_Tenancy_Architecture.md` — isolation architecture for T1-series tasks
- `04_Sentry_Onboarding_Flow.md` — UX spec for T2-series + T4-series
- `03_Sentry_Pricing_Business_Model.md` — pricing for T3-series

---

**Note:** Task статус, owner, due date нэмэлтэйгээр шинэчлэгдэнэ. Pilot launch-ын дараа сар бүр retrospective-д шинэчилнэ.
