[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (require database mocking)
    security: Security-related tests
    auth: Authentication and authorization tests
    api: API endpoint tests
    autolearner: Auto-learning system tests
