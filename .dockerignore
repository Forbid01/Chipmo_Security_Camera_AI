# VCS
.git
.gitignore
.github

# Python
.venv
venv
__pycache__
*.pyc
*.pyo
.pytest_cache
.ruff_cache
.mypy_cache

# Node
node_modules
security-web/node_modules
security-web/dist

# Env / secrets
.env
.env.*
!.env.example

# IDE
.vscode
.idea
*.swp

# Local artifacts
alerts/
faces/
*.log
test.db
.claude
.DS_Store

# Self-hosted VLM microservice — built separately from
# vlm_server/Dockerfile on a GPU host. Bundling it into the main image
# (Railway / Dockerfile) only bloats the build with unused code.
vlm_server/

# GPU image is a separate build target.
Dockerfile.gpu
